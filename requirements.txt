# Fallout Character Sheet Generator - Dependencies
#
# Core functionality (Markdown output) requires no dependencies.
#
# For HTML/PDF output, install optional dependencies:
#   pip install -r requirements.txt

# HTML output support
jinja2>=3.0

# PDF output support (includes jinja2 dependency)
weasyprint>=60.0
